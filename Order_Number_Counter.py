import csv

master_log = "DataSets/Subcontent/Order_log.csv"


def get_last_order_number(order_list):
    for order in order_list
        last_order_number = order_number_list[len(order_number_list) - 1]
        log_of_last_order.close()
        number_ = last_order_number[1]
        roll_number_up = int(number_) + 1
        new_number_ = roll_number_up
    return new_number_
